# Local build notes and personal scratch — not part of the published package.
docs/superpowers/
.DS_Store
BUILD_BRIEF.md
PUBLISHING.md

# Python bytecode — a directory-source plugin cache syncs the working tree, so
# anything left here ships into the installed plugin (EVIDENCE E-016).
__pycache__/
*.py[cod]

# Local runtime state and private release/audit artifacts.
.in_use
.pytest_cache/
.cache/
/ACGM-*-delivery/
/ACGM-install*/
/native-hook-evidence*/
/native-hook-evidence*.tar.gz
/rollback*/
/.governance/remote-path-guards.json
