# C-20260911-05: 四个提交推送至 GitHub main，本次不打 tag

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-05
- **起草于 / Drafted:** 2026-09-11T08:35Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0008
- **关联 HEAD / At commit:** c04b797
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

人的裁定原话："把这个更新推送到 GitHub"。

`git push origin HEAD:main` 推送 ab7b818、2e5e942、cdb9133、c04b797 四个提交，
`origin/main` 由 0ed61e6 前进至 c04b797。

**不打 tag**，因为远端当前零 tag：0.4.0 到 0.9.0 六个版本均未打过。本地仅存
v0.3.0-rc.1 与 v0.3.0-rc.4 两个从未推送的旧 tag。不打与现状一致；要改成打 tag，
是一条独立的发布纪律决定，不应搭在一次普通推送上顺手做掉。

## 依据 / Basis

- 推送前只读核查：4 个提交、11 个文件、328 增 12 删；`git ls-files` 确认无
  settings.local / .env / secret / token 类文件被跟踪；`.claude/settings.local.json`
  由全局忽略规则 `/Users/mac/.config/git/ignore:1` 排除。
- 待推 diff 中出现的本机绝对路径仅 `/Users/mac/Claude Code项目治理方法论`，
  不含真实姓名，与项目的化名发布立场不冲突。
- 推送后 `git ls-remote --heads origin` 返回 `refs/heads/main = c04b797`，
  `git log origin/main..HEAD` 为空。

## 未收敛的部分 / Still open

- 发布是否应当打 tag（仓库有 `claude plugin tag` 可用而从未使用过）—— 见 T-0010。
