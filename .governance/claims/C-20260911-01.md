# C-20260911-01: doctor 的 `cache matches source` 从自指比对改为对 marketplace 源目录比对

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-01
- **起草于 / Drafted:** 2026-09-11T08:20Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0001
- **关联 HEAD / At commit:** 0ed61e678aa8d6aed79af31f609eaee891d10f0e
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

候选集：(a) 不动，接受该检查在官方推荐调用方式下恒真；(b) 检测到自指时降级为 WARN；
(c) 检测到自指时回退到 marketplace 登记的源目录做真实比对。

收敛到 (c)，并保留 (b) 作为源目录不可得时的兜底。

人的裁定原话："如果很简单的话，比如说五分钟之内能做，那就做"。经评估为单文件约 20 行
shell、无既有 doctor 测试需同步修改，判定为简单，遂实施。

## 依据 / Basis

- `scripts/acgm-doctor.sh:28` —— `PLUGIN_DIR` 缺省解析为脚本自身所在目录的上一级。
- `scripts/acgm-doctor.sh:157-165` —— `install_path` 从 `installed_plugins.json` 读出。
- `scripts/acgm-doctor.sh:172-177`（修改前）—— 比对循环两侧分别是 `$install_path` 与
  `$PLUGIN_DIR`。当从缓存内调用时二者是同一棵树，`diff_count` 恒为 0。
- SessionStart 注入的提示正是让人从缓存路径调用该脚本，因此这条路径是默认路径而非边缘情况。
- 实测对照：从缓存调用得 `PASS cache matches source`；同一时刻用
  `--plugin-dir <仓库>` 调用得 `FAIL 3 file(s) differ`。同一份字节，两种结论。
- 修复后实测：从缓存调用得 `FAIL 4 file(s) differ from <local-marketplace-source>`，
  重装后同一调用得 `PASS cache matches source  matches <local-marketplace-source>`。

## 未收敛的部分 / Still open

- 该缺陷属于 ACGM 自身「配置已验证」这一状态的取证强度问题，是否值得在 `CASES.md`
  立案（一个自检脚本在其官方推荐调用路径下产出空洞绿灯），尚未裁定 —— 见 T-0003。
- 版本号是否需要从 0.9.0 提到 0.9.1，未裁定 —— 见 T-0004。
