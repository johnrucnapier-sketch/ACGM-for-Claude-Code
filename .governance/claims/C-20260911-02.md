# C-20260911-02: 插件重装走「卸载 + 安装」，不走 update

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-02
- **起草于 / Drafted:** 2026-09-11T08:20Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0002
- **关联 HEAD / At commit:** 0ed61e678aa8d6aed79af31f609eaee891d10f0e
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

候选集：(a) `claude plugin update acgm@acgm`；(b) `uninstall` 后 `install`。

收敛到 (b)。(a) 经实测在同版本号下为 no-op，无法刷新缓存——这一点 doctor 的失败提示
早已写明（"'update' is a no-op at the same version"），本次是实测复现而非引用。

人的裁定原话："如果你认为需要重装插件的话，现在就走一下重装"。

## 依据 / Basis

- `known_marketplaces.json` —— acgm 的 source 为 `directory`，path 指向本仓库。
  因此本机安装从不经过 GitHub 分发路径，重装即从本地工作树取字节。
- 重装前 `diff -r`：缓存与仓库差 4 个文件（README.md、CHANGELOG.md、
  .claude/settings.local.json、scripts/acgm-doctor.sh）。
- 实测 `claude plugin update acgm@acgm` 返回 `already at the latest version (0.9.0)`，
  缓存未变。
- 重装后 `diff -r` 仅剩 `Only in <仓库>: .git`，字节一致。
- 安装记录 `gitCommitSha` 由 6ad9667 更新为 0ed61e6。

## 未收敛的部分 / Still open

- **本次安装的 gitCommitSha 记的是 0ed61e6，但工作树当时含一处未提交改动
  （scripts/acgm-doctor.sh）。也就是说该记录描述不了缓存里的真实字节。** 这是本次
  操作引入的新的记录不实问题，需要在提交后重装一次才能消解 —— 见 T-0005。
