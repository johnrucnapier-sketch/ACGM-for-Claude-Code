# C-20260911-03: 从本地目录源安装时，顺序固定为「先提交，再重装」

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-03
- **起草于 / Drafted:** 2026-09-11T08:22Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0005
- **关联 HEAD / At commit:** ab7b818
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

候选集：(a) 接受安装记录里的 `gitCommitSha` 与缓存字节不对应；(b) 每次从本地目录源
安装前先把工作树提交干净，使该 sha 描述一个真实存在、内容对应的 commit。

收敛到 (b)，并作为此后从 directory 源安装 ACGM 的固定顺序。

人的裁定原话："这两个尾巴我没看懂，所以你觉得应该怎么做？按照你的要求来做吧" ——
属授权执行，非对本条内容的逐项确认。

## 依据 / Basis

- `known_marketplaces.json` —— acgm 的 source 为 `directory`，安装即从工作树取字节，
  不经 Git 对象库。因此工作树脏时，字节与任何 commit 都不对应。
- 本 session 实测：2026-09-11T08:15Z 的安装发生在工作树含未提交 doctor 改动时，
  `installed_plugins.json` 记录 `gitCommitSha: 0ed61e6`，而 `0ed61e6` 版本的
  `scripts/acgm-doctor.sh` 仍含自指缺陷 —— 记录与实物不符。
- 提交 ab7b818 后，同一条记录变为「指向一个不含本次修复的旧 commit」，不符更明显。

## 未收敛的部分 / Still open

无。该顺序不依赖版本号是否提升（T-0004 独立）。
