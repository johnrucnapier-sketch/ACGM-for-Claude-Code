# C-20260911-04: 自指缺陷立案为 Case 17 / E-033，版本提至 0.9.1，Git 陈旧引用清理

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-04
- **起草于 / Drafted:** 2026-09-11T08:30Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0003, T-0004, T-0006
- **关联 HEAD / At commit:** cdb9133
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

三条一并裁定，人的原话："那三条也按你的想法去做吧……我觉得你的判断是对的"。

- **T-0003** → 立案。写成 `CASES.md` 的 Case 17 / 案例 17（双语），并在 `EVIDENCE.md`
  登记 E-033（双语），成熟度 **Observed**。
- **T-0004** → 提至 0.9.1。理由是 doctor 的**行为**变了：同一次调用，同一批字节，
  裁决从 PASS 变成 FAIL。这不是文档改动。
- **T-0006** → 清理。prune 五个远端已删除的 remote-tracking ref，重设悬空的
  `origin/HEAD`，删除完全并入 HEAD 的本地 `master`。

## 依据 / Basis

- E-033 的主张刻意写得比案例窄：它是一条**报告纪律**（比对必须在输出里点名两侧），
  不是"所有自检都有此缺陷"这种普遍断言。按 `EVIDENCE.md` 的成熟度规则，单次观察
  只够 Observed，不够 Corroborated。
- 0.9.1 发布前本 Session 实跑 CI 定义的三项检查：`sh -n` 全部 shell 脚本通过；
  `python3 -m unittest discover -s tests` 在设与不设 `CLAUDECODE=1` 两种环境下
  各 56/56 OK；`claude plugin validate .` 返回 Validation passed。
- 删除 `master` 前实测 `git rev-list --count HEAD..master` = 0，
  `git merge-base --is-ancestor master HEAD` 成功——零独有提交。
- 重装后 `installed_plugins.json`：version 0.9.1、installPath 指向 `.../0.9.1`、
  gitCommitSha = cdb9133 = 仓库 HEAD；doctor 6/6 PASS。

## 未收敛的部分 / Still open

- **分支命名仍与内容不符**：当前工作分支叫 `feat/v0.4.0`，实际承载 0.9.1 并跟踪
  `origin/main`，是事实上的主干。这是结构放置类（③）的漂移，本次未处理 —— 见 T-0007。
- 三个提交（ab7b818、2e5e942、cdb9133）**尚未推送**，GitHub 上的 `main` 仍停在
  0ed61e6 —— 见 T-0008。
