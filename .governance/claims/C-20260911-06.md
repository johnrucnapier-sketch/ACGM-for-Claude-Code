# C-20260911-06: 远程执行按载荷判定，v0.9.2；误报率以本机历史实测校准

草案。由 agent 起草，**未经人确认**。它记录的是"一个开放线程闭合了"，不是"一件事被
决定了"——后者要人盖章才成立。

A draft, written by the agent and **not confirmed by a human**. It records that an
open thread closed. It does not establish that anything was decided.

- **编号 / Id:** C-20260911-06
- **起草于 / Drafted:** 2026-09-11T09:10Z
- **起草依据 / Closure signal:** human_ruling + entered_implementation
- **闭合线程 / Closes thread:** T-0011
- **关联 HEAD / At commit:** d79fd58
- **状态 / Status:** 待认定 / pending

## 收敛了什么 / What converged

人的裁定原话："现在做吧，因为我大量要使用 SSH 这个功能"。

候选集：(a) 把 `ssh`/`scp`/`rsync` 加进子串表（约 3 行）；(b) 读 ssh 的远程载荷，
递归套用同一套只读规则。

收敛到 (b)。(a) 被否决的理由不是它不管用，而是它在本操作者的使用强度下会把
`ssh rig 'uptime'` 一并拦住，且完全不触及 `bash X.sh` 那个真正的形状。

**门在此处的断言刻意写弱**：不是"这条是破坏性的"，而是"从这里看，这条和会造成破坏
的那种分不开"。这两句在本方法论里不是一回事。

## 依据 / Basis

- **上一轮的诊断是错的，且是在核实之前说出口的。** 原话是"门看到 ssh，看不见引号里
  的东西"。实测推翻：`ssh rig 'rm -rf x'` 被拦住。子串匹配作用于整条调用，引号不隔离。
  真正的缺口是 `bash run_quant.sh` 不含任何表内动词。
- 缺口非远程独有：重放确认 `bash run.sh`、`python train.py`、`make deploy`、
  `npm run migrate` 在本地同样不开火，**至今仍然如此**，本次未修。
- 误报率在本机 `~/.claude/projects/*/*.jsonl` 的 6157 条去重远程命令上实测，
  非估算：第一个可用版本新增被拒 **42%**（E-021 量级）→ 教会循环关键字/`sudo`/前置
  环境变量赋值后 **32%** → 补 `ss`/`pip show` 等只读动词后 **28%**。
- 测试挡下三处本会放行的漏洞：裸 `nvidia-smi`（会放行 `-pl` 改功耗墙）、`watch`、
  `timeout N`（后两者参数是任意命令）。
- 另有一条测试期望我写错了（以为 `sudo sshd -T` 该放行）。**改测试，未改代码**：
  `sshd -T` 只比启动守护进程多一个 flag。
- 发布前 CI 三项实跑：`sh -n` 全过、unittest 两种环境各 68/68 OK、
  `claude plugin validate .` 通过。

## 未收敛的部分 / Still open

- 本地的 `bash X.sh` / `make deploy` 同类缺口未修 —— 见 T-0012。
- 28% 这个数是**本操作者工作负载的属性，不是常数**；对他人的实际影响未知，无数据。
- 门本身对"命令携带的数据"仍会误伤：本 session 内一条只读 `grep`（搜索词含破坏性
  动词字面量）与一条 commit（正文含命令替换字面量）各被拦一次 —— 见 T-0013。
